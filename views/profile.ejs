<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
    <div class="container">
        <h1>Hi there, <%= user.username %></h1>
        <p>Followers: <%= user.followers.length %></p>
        <p>Following: <%= user.following.length %></p>

        <button id="followBtn" onclick="toggleFollow('<%= user._id %>')">Follow</button>
        <button id="unfollowBtn" onclick="toggleFollow('<%= user._id %>')" style="display: none;">Unfollow</button>

        <!-- Include necessary scripts or link to a JavaScript file -->
        <script>
            // Function to toggle follow/unfollow
            function toggleFollow(userId) {
                // Implement AJAX request to the server to follow/unfollow the user
                // Update button states based on the server response
            }
        </script>
    </div>
</body>
</html>
